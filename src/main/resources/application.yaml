spring:
  application:
    name: msa-invoices
  datasource:
    url: jdbc:postgresql://localhost:5431/invoices
    username: invoices
    password: invoices
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate